# Changelog

## Repository consolidation

- Added exact uploaded V9.2 and V12 research notebooks.
- Added two separately frozen runtime submission folders.
- Corrected the V9.2 repository directory layout so its unchanged `submission.py` can resolve `artifacts/model_config.json` and `artifacts/xxz_bell_prototypes.npz`.
- Preserved original uploaded ZIPs for provenance.
- Added public-safe validation, results summaries, architecture documentation, and CI.
